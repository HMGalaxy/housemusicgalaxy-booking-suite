# Galaxy Cue v5.0.0-alpha.18 — Mobile Inline Event Creation

- On phones, New Event now behaves like New Client.
- The event form opens inside the Events detail panel instead of a full-screen modal.
- The form remains single-column, scrollable with the page, and fully inside the mobile viewport.
- Desktop keeps the existing modal workflow.
