# Galaxy Cue v5.0.0-alpha.22

## Verified typography fix

- Added reliable workspace module identifiers instead of relying only on form selectors.
- Music Planner titles, card headings, labels, inputs, selects, textareas and buttons now use the CRM body font.
- Files / Upload Center title, upload area, file rows and controls now use the same CRM typography.
- Removed the visible Sansita-vs-Nunito mismatch on these two top-level destinations.
- Added a new cache-busting stylesheet version.
